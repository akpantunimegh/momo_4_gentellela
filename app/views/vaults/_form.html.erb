<%= form_for(@vault) do |f| %>
  <% if @vault.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vault.errors.count, "error") %> prohibited this vault from being saved:</h2>

      <ul>
      <% @vault.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password_token %><br>
    <%= f.text_field :password_token %>
  </div>
  <div class="field">
    <%= f.label :balance %><br>
    <%= f.text_field :balance %>
  </div>
  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.text_field :account_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
